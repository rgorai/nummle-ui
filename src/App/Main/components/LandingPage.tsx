import { APP_NAME } from '../../../utils/env'

const LandingPage = () => {
  return (
    <>
      <div>{`Welcome to hii zack ${APP_NAME}`}</div>
      <div> {'TEST'}</div>
    </>
  )
}

export default LandingPage
